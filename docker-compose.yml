version: '3.8'

services:
  recyclarr:
    image: ghcr.io/recyclarr/recyclarr
    container_name: recyclarr
    read_only: true
    tmpfs: /tmp
    security_opt:
      - no-new-privileges:true
    user: ${PUID}:${PGID}
    env_file:
      - .env
    environment:
      # Recyclarr scheduling
      - CRON_SCHEDULE=${CRON_SCHEDULE:-@daily}
      - RECYCLARR_CREATE_CONFIG=true
      - TZ=${TZ}
      # Required for main instances
      - RADARR_URL=${RADARR_URL}
      - RADARR_API_KEY=${RADARR_API_KEY}
      - SONARR_URL=${SONARR_URL}
      - SONARR_API_KEY=${SONARR_API_KEY}
      # Optional: Multiple instances (uncomment in .env to use)
      # - RADARR_4K_URL=${RADARR_4K_URL}
      # - RADARR_4K_API_KEY=${RADARR_4K_API_KEY}
      # - SONARR_ANIME_URL=${SONARR_ANIME_URL}
      # - SONARR_ANIME_API_KEY=${SONARR_ANIME_API_KEY}
    volumes:
      - ./recyclarr:/config
    networks:
      - ${DOCKER_NETWORK}

  language-tagger:
    build: ./language-tagger
    container_name: langarr-tagger
    read_only: true
    tmpfs: /tmp
    security_opt:
      - no-new-privileges:true
    depends_on:
      - recyclarr
    env_file:
      - .env
    environment:
      # Script configuration
      - RUN_MODE=schedule
      - CONFIG_PATH=/config/config.yml
      - TZ=${TZ}
      # Required for main instances
      - RADARR_URL=${RADARR_URL}
      - RADARR_API_KEY=${RADARR_API_KEY}
      - SONARR_URL=${SONARR_URL}
      - SONARR_API_KEY=${SONARR_API_KEY}
      # Optional: Multiple instances (uncomment in .env to use)
      # - RADARR_4K_URL=${RADARR_4K_URL}
      # - RADARR_4K_API_KEY=${RADARR_4K_API_KEY}
      # - SONARR_ANIME_URL=${SONARR_ANIME_URL}
      # - SONARR_ANIME_API_KEY=${SONARR_ANIME_API_KEY}
      # Optional: Testing/debugging
      # - DRY_RUN=false  # Set to true to preview changes without applying
    volumes:
      - ./language-tagger:/config:ro
    networks:
      - ${DOCKER_NETWORK}
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"

networks:
  ${DOCKER_NETWORK}:
    external: true
